function cv --wraps='cava -p ~/.cache/hellwal/cava' --wraps='cava -p ~/.cache/hellwal/cava.ini' --description 'alias cv=cava -p ~/.cache/hellwal/cava.ini'
    cava -p ~/.cache/hellwal/cava.ini $argv
end
